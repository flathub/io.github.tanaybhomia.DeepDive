app-id: io.github.tanaybhomia.DeepDive
runtime: org.gnome.Platform
runtime-version: '50'
sdk: org.gnome.Sdk
command: deepdive
finish-args:
  - --share=ipc
  - --socket=fallback-x11
  - --socket=wayland
  - --device=dri
modules:
  - name: deepdive
    buildsystem: meson
    sources:
      - type: git
        url: https://github.com/tanaybhomia/DeepDive.git
        tag: v1.0.4
        commit: 774ba58244171e6f1949f824895d69bbdd85ef3f
