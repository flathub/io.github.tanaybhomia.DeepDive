app-id: io.github.tanaybhomia.DeepDive
runtime: org.gnome.Platform
runtime-version: '50'
sdk: org.gnome.Sdk
command: deepdive
finish-args:
  - --share=ipc
  - --socket=fallback-x11
  - --socket=wayland
  - --device=dri
modules:
  - name: deepdive
    buildsystem: meson
    sources:
      - type: git
        url: https://github.com/tanaybhomia/DeepDive.git
        tag: v1.0.1
        commit: 3f2d70025eaec2a0e6041f1c2402c576c6735c2a
